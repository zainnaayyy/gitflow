// Create a logic to ping the server and check if there are change in the moodels and response with a flag
// refereing the changed model and invalidateTag in RTK.


const InvalidateBackendCache = () => {
  return {}
}

export default InvalidateBackendCache